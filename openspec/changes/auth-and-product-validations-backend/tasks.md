# Tasks: auth-and-product-validations-backend

> Generated by `sdd-tasks-m3` on 2026-06-03. Persists to Engram topic
> `sdd/auth-and-product-validations-backend/tasks` and to this file.
> Strict TDD active (`rules.apply.tdd: true`).
> Two commits: Commit 1 = cycles 1-3 (code), Commit 2 = cycle 4 (migration 0026). Cycle 5 is folded into Commit 1 (small DTO change).
> Migration verification is MANUAL (`run` + `:revert` on local PG, like migration 0025).

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~175 including tests |
| 400-line budget risk | Low |
| Chained PRs recommended | No (single PR, two commits) |
| Delivery strategy | ask-on-risk |
| Decision needed before apply | No |

### Work Units

| Unit | Goal | Commit | Notes |
|------|------|--------|-------|
| 1 | Cycles 1-3 + 5: env-config + unlock counter reset (TWO paths) + customer.email handler check + DTO exposure | Commit 1 | All code, no migration. Reversible in isolation. |
| 2 | Cycle 4: migration 0026 (UNIQUE INDEX on EMA_CUS) | Commit 2 | Depends on Commit 1. Reversible (down() drops the index). |

Decision needed before apply: No
Chained PRs recommended: No
Chain strategy: pending
400-line budget risk: Low

## Phase 1: Code (Commit 1) — strict TDD

### Cycle 1: `unlockUser()` counter reset (BOTH code paths)

The design's Decision 11: the bug exists in TWO code paths. Test + fix BOTH.

- [x] 1.1 **RED (auth.service path)** `src/infrastructure/services/auth.service.spec.ts` — add test: Given user with `failedLoginAttempts=3` and `STA_USR=BLOCKED`, When `unlockUser(userId)` is called, Then `updateFailedLoginAttempts(userId, 0)` is called AND `STA_USR` becomes ACTIVE. Run `npm run test -- auth.service` → expect FAIL.
- [x] 1.2 **GREEN (auth.service path)** `src/infrastructure/services/auth.service.ts:156-161` — add `await this.userRepository.updateFailedLoginAttempts(id, 0)` after the `user.unlock()` call. Run test → expect PASS.
- [x] 1.3 **RED (CQRS handler path)** `src/application/cqrs/user/commands/unlock-user/unlock-user.handler.spec.ts` (create if not exists) — add test: Given the same scenario, When `UnlockUserCommand` is executed, Then the counter is reset. Run test → expect FAIL.
- [x] 1.4 **GREEN (CQRS handler path)** `src/application/cqrs/user/commands/unlock-user/unlock-user.handler.ts` — add the same counter reset. Run test → expect PASS.
- [x] 1.5 **REFACTOR** `rg "unlockUser|UnlockUserCommand" src/ | rg -v "spec.ts" | rg "updateFailedLoginAttempts" -c` → confirm 2 hits (both paths wired). `npm run test` → 158+N green, build clean.

### Cycle 2: env-configurable threshold

- [x] 2.1 **RED** `src/infrastructure/services/auth.service.spec.ts` — add test: Given `process.env.AUTH_MAX_FAILED_ATTEMPTS = 10`, When the service is constructed, Then the threshold is 10. Run test → expect FAIL.
- [x] 2.2 **GREEN** `src/config/configuration.ts` — add `auth: { maxFailedAttempts: parseInt(process.env.AUTH_MAX_FAILED_ATTEMPTS ?? '5', 10) }`. `src/infrastructure/services/auth.service.ts:39` — replace `MAX_FAILED_ATTEMPTS = 3` with reading from `this.configService.get('auth.maxFailedAttempts')` (inject ConfigService in constructor). `.env.example` — add `AUTH_MAX_FAILED_ATTEMPTS=5`. Run test → expect PASS.
- [x] 2.3 **REFACTOR** ensure ConfigService is properly injected; check no other consumers of `MAX_FAILED_ATTEMPTS` constant. `npm run test` + `npm run build` → green.

### Cycle 3: Customer email uniqueness (handler)

- [x] 3.1 **RED (CreateCustomerHandler)** `src/application/cqrs/customer/commands/create-customer/create-customer.handler.spec.ts` — add test: Given a customer with `email = foo@bar.com`, When `CreateCustomerCommand({email: 'foo@bar.com', ...})` is executed, Then `DuplicateCustomerFieldsException` is thrown with `errors.email` populated. Run test → expect FAIL.
- [x] 3.2 **GREEN (CreateCustomerHandler)** `src/application/cqrs/customer/commands/create-customer/create-customer.handler.ts` — add an email-uniqueness check that mirrors the existing cedula check at line 13-20. Throw `DuplicateCustomerFieldsException` (existing exception class) with `{ errors: { email: 'duplicate' } }`. Run test → expect PASS.
- [x] 3.3 **RED (UpdateCustomerHandler)** `src/application/cqrs/customer/commands/update-customer/update-customer.handler.spec.ts` — add test: Given two customers, When customer A is updated to take B's email, Then exception. Run test → expect FAIL.
- [x] 3.4 **GREEN (UpdateCustomerHandler)** `src/application/cqrs/customer/commands/update-customer/update-customer.handler.ts` — add the email-uniqueness check, but EXCLUDE the customer being updated from the lookup (so updating without changing email works). Run test → expect PASS.
- [x] 3.5 **REFACTOR** `rg -n "DuplicateCustomerFieldsException" src/` → confirm both handlers throw the same exception. `npm run test` + `npm run build` → green.

### Cycle 4 (folded into Commit 1): UserResponseDto exposure

- [x] 4.1 **RED** `src/application/dto/user/user-response.dto.spec.ts` (create if not exists) — add test: `fromEntity({...user, failedLoginAttempts: 3})` → result has `failedLoginAttempts: 3`. Run test → expect FAIL.
- [x] 4.2 **GREEN** `src/application/dto/user/user-response.dto.ts` — add `failedLoginAttempts: number` field and map from entity. Run test → expect PASS.
- [x] 4.3 **REFACTOR** verify the `/users` endpoint (not `/auth/users`) now exposes the field. `npm run test` + `npm run build` → green.

## Phase 2: Migration (Commit 2) — manual TDD gate

- [ ] 5.1 **Pre-check (manual)** — run the script: `SELECT EMA_CUS, COUNT(*) FROM CUSTOMERS WHERE EMA_CUS IS NOT NULL AND DEL_AT IS NULL GROUP BY EMA_CUS HAVING COUNT(*) > 1;` against the local PG. If rows returned, document them; do NOT proceed without a cleanup plan. If zero rows, proceed.
- [ ] 5.2 Create `src/infrastructure/database/migrations/1800000000026-AddCustomerEmailUnique.ts`. Class `AddCustomerEmailUnique1800000000026`, `name = 'AddCustomerEmailUnique1800000000026'`. `up()`: add a UNIQUE INDEX `IDX_CUS_EMAIL` on `CUSTOMERS(EMA_CUS)`. **Multi-engine pattern**: `dbType === 'postgres' ? await qr.query('CREATE UNIQUE INDEX ...') : (oracle ? ... : ...)`. **Idempotency guard**: check `await qr.getTable('CUSTOMERS')?.getIndex('IDX_CUS_EMAIL')` exists before creating. `down()`: `await qr.dropIndex('CUSTOMERS', 'IDX_CUS_EMAIL')`.
- [ ] 5.3 **Manual gate**: `npm run typeorm:migration:run` against local PG → verify IDX_CUS_EMAIL exists via `psql \d CUSTOMERS` → `npm run typeorm:migration:revert` → verify index gone → re-run → verify back. **Multi-engine note**: this is the first migration touching an Oracle/MySQL-only feature; if those engines are unreachable, document and accept PG-only verification (same as migration 0025).
- [ ] 5.4 **REFACTOR** `rg "DuplicateCustomerFieldsException" src/` — confirm no orphan references; `npm run test` + `npm run build` → green.

## Phase 3: Final verification

- [ ] 6.1 `npm run test` → all suites pass (target: 158 + new tests for cycles 1-4, expected +20-30 new tests).
- [ ] 6.2 `npm run build` → clean compile.
- [ ] 6.3 `rg "MAX_FAILED_ATTEMPTS = 3|MAX_FAILED_ATTEMPTS = 5" src/` → 0 hits (no hardcoded threshold remains).
- [ ] 6.4 `rg "unlockUser|UnlockUserCommand" src/ --type ts` → exactly 2 paths (`auth.service` + `UnlockUserHandler`) and both call `updateFailedLoginAttempts(..., 0)`.
- [ ] 6.5 `git log --oneline -3` → shows 2 new commits in order: code (cycles 1-4) first, migration (cycle 5) second, on top of the existing `04f6ae7`.

## Open questions

None. All decisions resolved (obs 2226). Design's 5 cycles are reflected; design's 2-path unlock bug is reflected.
