# Tasks: dashboard-kpis-backend

> Generated by `sdd-tasks-m3` on 2026-06-04. Persists to Engram topic `sdd/dashboard-kpis-backend/tasks` (obs 2259) and to this file.

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~60-120 |
| 400-line budget risk | Low |
| Chained PRs recommended | No |
| Suggested split | Single PR |
| Delivery strategy | ask-on-risk |
| Chain strategy | pending |

Decision needed before apply: No
Chained PRs recommended: No
Chain strategy: pending
400-line budget risk: Low

### Suggested Work Units

| Unit | Goal | Likely PR | Notes |
|------|------|-----------|-------|
| 1 | Align the dashboard KPI contract and aggregates | PR 1 | Single backend slice; tests included; no frontend changes. |

## Phase 1: Foundation / Contract

- [x] 1.1 Narrow `src/application/dto/dashboard/dashboard-stats.dto.ts` to `ventasDelDia`, `ventasDelMes`, `totalFacturas`, and `productosConStockBajo` only.
- [x] 1.2 Update `src/domain/query-services/dashboard.query-service.interface.ts` so `getStats()` returns the canonical four-field KPI projection.
- [x] 1.3 Add `src/domain/constants/inventory.constants.ts` with `LOW_STOCK_THRESHOLD = 10`, then replace the hardcoded low-stock check in `src/infrastructure/repositories/dashboard.repository.ts`.

## Phase 2: Core Implementation

- [x] 2.1 Rework `src/infrastructure/queries/dashboard/dashboard.query.service.ts` to return only the four KPI fields, with local-time start-of-day/start-of-month windows.
- [x] 2.2 Map query results to zero-safe numbers (`COALESCE` / `Number(...) || 0`) so empty aggregates never return `null`.
- [x] 2.3 Keep `GET /dashboard/estadisticas` on the CQRS path via `src/application/cqrs/dashboard/queries/get-dashboard-stats/get-dashboard-stats.handler.ts` and preserve the optional `branchId` filter.
- [x] 2.4 Decide whether `src/application/use-cases/dashboard/get-dashboard-stats.use-case.ts` stays as reference-only or is deleted; if kept, align it to the narrowed DTO so it no longer advertises the old contract.

## Phase 3: Testing / Verification

- [x] 3.1 Update `src/presentation/controllers/dashboard.controller.spec.ts` to assert the endpoint returns exactly the four Spanish numeric fields and passes through `branchId`.
- [x] 3.2 Add `src/infrastructure/queries/dashboard/dashboard.query.service.spec.ts` for raw aggregate mapping, empty-db zeros, low-stock threshold, and local-midnight boundary behavior.
- [x] 3.3 Extend `src/application/cqrs/dashboard/queries/get-dashboard-stats/get-dashboard-stats.handler.spec.ts` to verify the handler delegates to the query service and preserves the branch filter.
- [ ] 3.4 Run `npm run test` and `npm run build` to confirm the backend contract fix is green.
- [x] 3.5 Refresh the Swagger summary/description in `src/presentation/controllers/dashboard.controller.ts` so it documents the four KPI cards, not the old customers/products/revenue payload.
